import React from 'react';
import PublicContractView from './PublicContractView';

/** Public signing page for recruitment (user) digital contracts. */
const PublicRecruitmentContractView: React.FC = () => {
  return <PublicContractView recruitmentMode />;
};

export default PublicRecruitmentContractView;
