/**
 * Dashboard AI assistant (ai-notifications edge function + public_messages + AIAssistantBox).
 *
 * Set to `false` to disable automatic fetches from Dashboard and any consumer that checks this flag.
 * The archived UI lives in `src/components/dashboard/DashboardAiSuggestionsArchive.tsx` (not mounted).
 */
export const DASHBOARD_AI_NOTIFICATIONS_FETCH_ENABLED = false;
