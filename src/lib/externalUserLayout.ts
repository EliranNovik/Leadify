/** Full-width content shell for external user pages (no centered `container` max-width). */
export const EXTERNAL_USER_PAGE_INNER =
  'w-full px-4 py-6 md:px-6 lg:px-8 xl:px-12';

/** Matches page inner padding for the external user top navbar on desktop. */
export const EXTERNAL_USER_HEADER_PADDING = 'md:px-6 lg:px-8 xl:px-12';
